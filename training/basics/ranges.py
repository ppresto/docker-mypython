#!/usr/bin/env python3.7

#Ranges
#Ref: https://docs.python.org/3/library/stdtypes.html#range

print(range(0,10))  #ranges are ordered lists that can be calculated on the fly so use little mem initially
print(list(range(10))) # convert directly into list.  great for ordered sequence
print(list(range(1,12,2)))  # Supports step value
